# DATA-NEEDED — outstanding clinic data gaps

Auto-regenerated during Phases 3–5. Do not fill values by hand here; supply
the data via the intake form or research file and let the ingestion step
update the clinic JSON.

Grouped by clinic. `source` says who can supply it: `web` (publicly
discoverable — run Instrument B) or `client` (must come from the clinic —
send Instrument A).

| clinic slug | field | build-blocking? | source | requested on | status |
|---|---|---|---|---|---|

_No entries yet — no clinic JSONs exist. First entries will be generated in
Phase 3 when `/data/clinics/marwa-deshmukh.json` is created._
